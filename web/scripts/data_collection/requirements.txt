# Dependencies for the data-collection / ingestion pipeline
# (create_tables.py and populate_db.py). Separate from the API runtime so the
# heavy data-science stack isn't required to serve the app.

pandas>=2.0
numpy>=1.24
requests>=2.31
beautifulsoup4>=4.12

# Needed by populate_db.py to write into the Alembic-managed Postgres schema.
SQLAlchemy>=2.0
psycopg[binary]>=3.1
python-dotenv>=1.0
